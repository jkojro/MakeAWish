

<h3>Daj się poznać.</h3>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= devise_error_messages! %>
  
  <div class="form-group">
    <%= f.text_field :name, placeholder: "Imię i Nazwisko", autofocus: true, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= f.email_field :email, placeholder: "Email. Szanujemy Twoją prywatność, nie musi być prawdziwy.", autofocus: true, class: 'form-control' %>
  </div>

  <div class="form-group">
    <% if @minimum_password_length %>
    <em>(Minimum <%= @minimum_password_length %> znaków)</em>
    <% end %><br />
    <%= f.password_field :password, placeholder: "Hasło, ono musi być prawdziwe.", autocomplete: "off", class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= f.password_field :password_confirmation, placeholder: "Musisz też naprawdę hasło potwierdzić.", autocomplete: "off", class: 'form-control' %>
  </div>

  <div class="actions">
    <%= f.submit "Zarejestruj" %>
  </div>
<% end %>


