<p id="notice"><%= notice %></p>

<h2><%= link_to 'Lista Wishy', home_wishlist_path %></h2>

<table>
  <tbody>
    <% @wishes.each do |wish| %>
      <tr>
        	<td><%= link_to wish.name, wish %></td>
			<% if (wish.reciever == current_user) %>
			
			<% end %>	
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Wish', new_wish_path %>

