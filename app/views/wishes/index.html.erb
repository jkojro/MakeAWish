
<h2><%= link_to "Spełnij Wisha!" %></h2>

<table>
  <tbody>
    <% @wishes.each do |wish| %>
     
			<% if ((wish.reciever == current_user.name) || (wish.reciever == current_user.email)) %>
				<%= link_to "Wish from  #{wish.user.name}", wish, class: "btn btn-danger btn-outline"  %></td>
			<% end %>	
    
    <% end %>
  </tbody>
</table>

